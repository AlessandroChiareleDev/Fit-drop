services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fitdrop
      POSTGRES_PASSWORD: fitdrop
      POSTGRES_DB: fitdrop
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
